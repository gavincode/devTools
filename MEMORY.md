# MEMORY.md

## 长期项目上下文

- 2026-08-08:项目初始化。目前包含 4 个工具:JSON 格式化、Base64 编解码、UUID 生成、时间工具。
- 设计基调:深色主题,主色 `#38bdf8`(accent)、辅助色 `#818cf8`(accent-2),背景 `#0f172a`。
- 时间工具使用固定时区映射(CN/UTC/IST),未使用 `Intl` 的 IANA 全量时区列表。
- 2026-08-08:页面整体改版。共享样式升级(导航、分段控件、结果面板、状态徽章、toast、footer);
  首页新增工具搜索(`/` 快捷键聚焦);工具页统一页头布局并新增复制、快捷键、状态提示;
  UUID 支持批量与大小写;时间工具新增"当前时间"与 Unix 时间戳。
- 2026-08-08:修复时间工具"目标时间"区域。根因:模板字符串缩进在 `white-space: pre-wrap`
  下被渲染成空白行,把时区卡片撑高(196px);且输出区把输入时间回显,与卡片内容重复。
  处理:成功态改为直接展示三张时区卡片(DOM 构建避免空白文本节点),源时区卡片改为
  "原时区"标签而非 55% 透明度;错误态使用独立提示行。

## 待办 / 已知限制

- 时间工具仍只支持 CN/UTC/IST 三个固定时区,后续可扩展为 `Intl.supportedValuesOf('timeZone')` 全量选择。
- 首页搜索为纯前端过滤,工具数量增多后可考虑分组/分类。
- 教训:在 `white-space: pre-wrap` 容器内用带缩进的模板字符串渲染内容会产生空白行,
  页面新内容优先用 DOM API 构建或显式设置 `white-space`。
