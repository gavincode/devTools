# AGENTS.md

## 项目概览

- 项目名:Developer Tools Hub(开发者工具集)
- 类型:静态站点,部署到 GitHub Pages(仓库根目录发布)
- 技术栈:原生 HTML / CSS / JavaScript,无构建工具、无依赖
- 创建/更新日期:2026-08-08

## 目录结构

- `index.html` — 首页:工具入口展示
- `styles.css` — 共享样式
- `tools/<tool-name>/index.html` — 每个工具一个独立页面

## 约定

- 页面语言 `zh-CN`,UI 文案使用中文
- 新增工具:在 `tools/` 下新建子目录,页面引用 `../../styles.css`,并在首页卡片区添加入口
- 工具脚本放在各页面内联 `<script>` 中,保持零依赖
- 样式优先复用 `styles.css` 中的设计变量;页面专属样式用页内 `<style>`
- 所有工具必须纯前端运行,数据不离开浏览器

## 验证

- 无测试框架;验证方式:浏览器打开各页面做功能与视觉检查
- 检查点:首页卡片跳转、工具输入/输出、移动端布局、文字溢出
